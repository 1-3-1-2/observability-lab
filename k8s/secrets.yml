# IMPORTANTE: Este fichero es una plantilla — los valores reales NO van en git
# Para crear el secret en el clúster usa:
# kubectl create secret generic observability-secrets \
#   --from-literal=postgres-password=<PASSWORD> \
#   --from-literal=rabbitmq-password=<PASSWORD> \
#   --from-literal=jwt-secret=<SECRET>
apiVersion: v1
kind: Secret
metadata:
  name: observability-secrets
type: Opaque
stringData:
  postgres-password: "CHANGE_ME"
  rabbitmq-password: "CHANGE_ME"
  jwt-secret: "CHANGE_ME"
